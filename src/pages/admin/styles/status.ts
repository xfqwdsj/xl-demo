import { css } from "lit";

export const statusStyles = css`
  .text-primary {
    color: var(--primary);
  }

  .text-error {
    color: var(--error);
  }

  .extra {
    font-size: 12px;
  }

  .extra.success {
    color: var(--success);
  }

  .extra.warning {
    color: var(--warning);
  }

  .extra.error {
    color: var(--error);
  }

  .extra.primary {
    color: var(--primary);
  }
`;
